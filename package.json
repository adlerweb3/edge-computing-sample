{
  "name": "edge-computing-sample",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.18.0",
    "typescript": "^4.8.4",
    "wrangler": "2.1.13"
  },
  "private": true,
  "scripts": {
    "local": "wrangler dev --local",
    "start": "wrangler dev",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "@upstash/redis": "^1.15.1",
    "worktop": "^0.7.3"
  }
}
